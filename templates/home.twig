{% extends 'base.twig' %}

{% block body %}
    <h1>Liste des projets</h1>

    {% for value in req %}
        <h2>{{ value[0]['id'] }}</h2>
        <p>{{ value[0]['name'] }}</p>
    {% endfor %}

{% endblock %}
